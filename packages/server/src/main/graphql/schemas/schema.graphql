type Query {
  login(username: String, password: String!, email: String): AuthenticateResponse!
  getProfileBySlug(username: String): User!
}

type Mutation {
  register(
    email: String!
    password: String!
    passwordConfirm: String!
    username: String!
  ): AuthenticateResponse!

  createPostType(description: String!, file: [Upload]!): Post!
  getAllPostsType(description: String!, file: [Upload]!): [Post]!
}

scalar Upload

type File {
  filename: String!
  mimetype: String!
  encoding: String!
}

type AuthenticateResponse {
  token: String!
  user: User!
}

scalar Date

type User {
  idSerial: Int!
  username: String!
  email: String!
  createdAt: Date!
  updatedAt: Date!
  avatar: Media
  avatarId: Int
  id: ID!
}

type Media {
  idSerial: Int!
  url: String!
  postId: Int
  createdAt: Date!
  updatedAt: Date!
  id: ID!
}

type Post {
  idSerial: Int!
  user: User
  userId: Int
  description: String
  medias: [Media]!
  createdAt: Date!
  id: ID!
  updatedAt: Date!
}
