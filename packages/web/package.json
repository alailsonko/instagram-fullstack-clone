{
  "name": "@tracking-system/web",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p ./tsconfig.build.json"
  },
  "dependencies": {
  }
}